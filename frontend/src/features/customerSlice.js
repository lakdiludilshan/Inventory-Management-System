import {createSlice, nanoid} from '@reduxjs/toolkit';

const intialState = {
    customer: {},
}

export const customerSlice = createSlice({
    name: 'customer',
    intialState,
    reducers: {
        
    }
})